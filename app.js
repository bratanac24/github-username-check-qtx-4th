let darkMode = () =>{
    mode = document.getElementById("body");
    mode.classList.add("dark-mode");
}
let lightMode = () =>{
    mode = document.getElementById("body");
    mode.classList.remove("dark-mode");
}